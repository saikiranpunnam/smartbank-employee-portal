apiVersion: v1
kind: Service
metadata:
  name: smartbank-portal-svc
  namespace: smartbank
spec:
  selector:
    app: smartbank-portal
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080   # Access via NodeIP:30080
  type: NodePort
